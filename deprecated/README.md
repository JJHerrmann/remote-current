# Deprecated designs

Previous iterations of the RemoteCurrent board, kept for reference. These are
not maintained.

## `index.html` — original board (v1)

The first live board: warm-paper palette, green accent, single-column list with
an advanced-filter panel. Read the same `data/jobs.json` as the current site.
Retired when the "signal desk" layout replaced it at the site root.

Still deployed at `/deprecated/` so shared links keep resolving.
